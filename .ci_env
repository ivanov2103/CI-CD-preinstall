#URL-адрес проекта разработки, необходимо переопределить на URL того проекта, для которого устанавливаем параметры CI/CD.
PROJECT_URL="https://gitlab.example.com/group/subgroup/example.project"
#Путь к секретам vault. Переменная VAULT_ROOT_PATH переопределяется только в том случае, если секрет с учетыми данными Harbor сохранен по другому пути.
#В этом случае, необходимо будет изменить и значение переменной CI_VAULT_TOKEN - указать токен с правами доступа к новому хранилищу секрета.  
VAULT_ROOT_PATH="secret/path"
#Используемая версия helm.
HELM_VER="2.12.3"
#Используемая версия kubectl.
KUBECTL_VER="1.12.3"